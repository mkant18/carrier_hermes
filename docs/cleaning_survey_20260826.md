# Artifact Cleaning Survey — 2026-08-26

**Tier 4 Silent Running survey. Read-only. No files deleted or moved.**

## Summary

| Category | Count | Est. disk impact |
|---|---|---|
| Safe to delete | 34 items (25 `.grok_bak` profile backups, 5 `__pycache__/*.pyc`, 1 backup manifest, resolved human-input-response pairs — see detail) | Low (~200 KB; pyc/bak are small text/binary files, no large blobs found) |
| Review needed | 8 stale `.worktrees/t_*` + matching `.git/worktrees` entries, 7 resolved `human_input_responses/*` JSON+brief pairs, `docs/reviews/openai_oauth_fleet_review.md` (only inside a worktree, not on main), `ob1_brain/.venv` (large, regenerable) | Medium-High (`.venv` alone likely several hundred MB; worktrees hold full working-copy checkouts) |
| Keep | Everything else surveyed: active scripts, active cron-referenced scripts, active carrier state, active logs, `main` branch data | — |

No `.tmp`, `.old`, or `*.orig` files were found anywhere in scope. No dangling `DISPATCH_LOCK` or `SILENT_RUNNING_BRAKE` files exist under `carrier/` (governor is actively cycling — `silent_running_state.json` shows `phase: running`, updated seconds before this survey ran, so no stale control files present to flag).

---

## 1. Safe to delete

### 1a. Grok-disable profile backups (25 files, `AppData/Local/hermes/profiles/*/config.yaml.grok_bak`)
Created by `scripts/disable_grok_use_sonnet.py` as a `--restore`-capable backup before it temporarily swapped `xai-oauth/grok-4.5` → `anthropic/claude-sonnet-4-6` across all 25 bot profiles. `docs/ops-grok-disable.md` explicitly documents these as "local-only backups" not meant to be committed, restorable via `--restore`, and safe to delete once the toggle is no longer needed.
- Files: `profiles/{api_watcher,calendar_manager,chief_of_staff,code_auditor,coding_lt,email_drafter,email_reader,finance_reader,firstmate,git_yeoman,hermes_ai_explorer,knowledge_lt,lockbox,maintenance_lt,marshal,obsidian_archivist,ops_lt,passive_watch,patch_writer,pr_reviewer,repair_planner,research_agent,subscription_watcher,todoist_manager,vault_librarian}/config.yaml.grok_bak`
- Companion manifest: `AppData/Local/hermes/carrier/grok_disable_backup.json` (maps live config → backup path; only useful while the `.grok_bak` files exist)
- **Caveat before deleting:** confirm the grok-disable toggle is *not currently active* (i.e., live configs still show `xai-oauth`/Grok, not the swapped Sonnet values) — if the toggle is still engaged, these are the only way to `--restore` back to Grok. Quick check: `grep -l "provider: xai-oauth" profiles/*/config.yaml`. If most profiles still show xai-oauth as primary, the backups are stale leftovers of a since-abandoned or already-reverted operation and are safe to delete.

### 1b. Compiled Python cache (5 files, `AppData/Local/hermes/scripts/__pycache__/*.pyc`)
Auto-regenerated bytecode cache for `fleet_checkin.py`, `ollama_rescue.py`, `silent_running_ladder.py`, `silent_running_broadcast.py`, `silent_running_common.py`. Standard `__pycache__` content — always safe to delete, Python regenerates on next import. Not `.gitignore`-relevant since these aren't tracked.

---

## 2. Review needed

### 2a. Stale `.worktrees/` directories (8 dirs) + matching `.git/worktrees/` metadata
`carrier_hermes/.worktrees/t_bc0dfa8a`, `t_db323fc2`, `t_87d24016`, `t_9b22664a`, `t_624bb21b`, `t_3898ab8c`, `t_194e3cb6`, `t_152c0930` — each a full git worktree checkout tied to a Kanban task branch (`wt/t_*`).

Kanban task status for each:
| Worktree | Task status | Notes |
|---|---|---|
| t_bc0dfa8a | done | Review doc lives only here (`docs/reviews/openai_oauth_fleet_review.md`) — not yet merged to `main`. **Do not delete until confirmed merged/copied to main.** |
| t_db323fc2 | done | share_subscriptions.py edit; superseded, work merged per t_c4a18c7e / t_e9dd9dbc verification comments |
| t_87d24016 | done | billing_guard.py edit; superseded per same verification chain |
| t_9b22664a | done | or_billing_policy.py edit; superseded per same verification chain |
| t_624bb21b | done | Shipwright smoke test; artifacts were placeholder paths (`path/to/audit_report_<date>.md`), nothing real written |
| t_3898ab8c | done, but flagged | Helm verification FAILED for PR #10 — chief_of_staff commented "reported artifacts do not exist." Branch `helm/resolve-pr-10-11-v2` may still hold unmerged/needed work. **Do not delete — needs LT/Helm re-verification first.** |
| t_194e3cb6 | done | Contentious history — a Mate sub-task destructively overwrote `or_billing_policy.py` mid-flight; recovered via `git restore --source=deef4ce`. Worktree may contain the corrupted intermediate state; verify branch tip matches recovered content before pruning. |
| t_152c0930 | **blocked**, actively crash-looping | Live/in-progress (Smart Router design spec) — 70+ crash-restart attempts recorded, still assigned. **Not stale — actively contested, exclude from cleanup entirely.** |

Reasoning for "review" not "safe": `wt/t_bc0dfa8a`, `wt/t_db323fc2`, `wt/t_87d24016`, `wt/t_9b22664a` all point to the same merge-base commit (`af15a107...`) as several others, suggesting a shared branch point, but none of these branches were found in `origin/*` refs (only `origin/wt/t_e9dd9dbc`, `origin/wt/t_bc0dfa8a`, `origin/wt/t_0eeee5bf`, `origin/wt/t_194e3cb6`, `origin/wt/t_18ce6386`, `origin/wt/t_8bdbad76` are pushed). Local-only branches `t_db323fc2`, `t_87d24016`, `t_9b22664a`, `t_624bb21b`, `t_3898ab8c`, `t_152c0930` have **no remote copy** — pruning the worktree AND local branch would permanently lose that history unless the parent task's changes are confirmed merged into `main` first.
- **Recommendation:** LT should verify `main` already contains the substantive changes from t_87d24016/t_9b22664a/t_db323fc2/t_bc0dfa8a (billing_guard.py, or_billing_policy.py, share_subscriptions.py already show the expected content in the main worktree per this survey — see confirmed greps below), then `git worktree remove` + `git branch -d wt/t_*` for the 6 non-live, non-flagged ones. Leave `t_3898ab8c` and `t_152c0930` for LT decision.

Confirmed via grep against the live (non-worktree) tree — the target changes ARE present on the checked-out branch:
- `scripts/billing_guard.py:89` → `SUB_ONLY = ("anthropic", "claude", "xai", "grok", "openai", "openai-oauth", "chatgpt")` ✅ matches t_87d24016's spec
- `scripts/or_billing_policy.py:234` → `{"anthropic", "xai-oauth", "openai-codex", "openai-oauth"}` ✅ matches t_9b22664a's spec
This is evidence (not proof of `main` branch state specifically) that the work landed somewhere reachable; LT should confirm the currently checked-out branch is in fact `main` before authorizing worktree deletion.

### 2b. Resolved `human_input_responses/` pairs (Helm auto-resolver artifacts, `AppData/Local/hermes/carrier/human_input_responses/`)
7 task-id-keyed JSON+brief `.md` pairs (`t_7be1a291`, `9bd1826d-9280-48e1-bd39-cd7dc03bad01`, `t_d74b8dae`, `ed607dca-a9ea-4efb-9625-f419b028e1d1`, `t_cf5a186b`, `t_62e8ced6` + a duplicate `task_62e8ced6_input.json`). These are Probe's research recommendations written back for Helm to post to the originating Kanban card as a comment/decision. All correspond to tasks that appear resolved/terminal in Kanban history (openai_oauth review chain, PR #5/#10 merges, SOUL.md write-guard requests). They're an audit trail of past auto-resolutions, not live state.
- **Recommendation:** Keep as an audit trail for now (small text files, low cost), OR archive to a dated subfolder if LT wants `human_input_responses/` to only contain currently-open items. Not "safe to delete" outright since they document Helm decision provenance; LT call.

### 2c. `ob1_brain/.venv` (`AppData/Local/hermes/carrier/ob1_brain/.venv/`)
A full Python virtualenv (numpy, cryptography, starlette, mcp, httpx, pywin32, etc. — dozens of packages) vendored inside the carrier state directory rather than gitignored/rebuildable-only. This is by far the largest disk consumer found in the survey scope (uv.lock present, so it's regenerable via `uv sync`).
- **Recommendation:** Confirm `ob1_brain` service is still active (yes — `ob1_brain.log` has entries from today, `carrier_ob1_watchdog.py` exists) before touching. If regeneration via `uv sync` is fast/reliable, this `.venv` doesn't need survey flagging as deletable, but LT should confirm it's excluded from any backup/sync jobs (it's disk weight with zero unique data — fully reproducible from `uv.lock` + `pyproject`).

---

## 3. Keep

- **All `scripts/` in both `carrier_hermes/scripts/` and `AppData/Local/hermes/scripts/`** — every script sampled either appears in `cron/jobs.json` (`fleet_checkin.py`, `blocked_task_watchdog.py`, `helm_input_resolver.py` + `helm_input_watchdog.py` monitor, `coding_stall_watcher.py`, `maintenance_preflight.py` monitor, `silent_running_governor.py`, `silent_running_checkpoint.py`, `kanban_dependency_gate.py`, `ollama_rescue_watchdog.py`, `carrier_gateway_watchdog.py`, `silent_running_gate.py` monitor) or is referenced by an active doctrine/skill doc (`silent-running` skill → `silent_running_ladder.py`, `silent_running_memory.py`, etc.) or by `maintenance_lt.md` (`maintenance_dispatch.py`). No orphaned/unused scripts were identified in the 91-file Hermes-home scripts dir or the ~180-file repo scripts dir within survey scope. `sr_*.py` one-off diagnostic scripts (`sr_assess.py`, `sr_check_crashed.py`, etc.) look like ad-hoc Silent Running debugging helpers with no cron/skill references — **flagging as a "review" candidate for next survey cycle**, not resolved here since scope was time-boxed to the fixed checklist above; not added to Safe/Review tables above because their purpose/authorship couldn't be confirmed in this pass (recommend LT spot-check before next cleaning survey includes them formally).
- **`carrier/*.db`, `*.db-wal`, `*.db-shm`** (`peers.db`, `webhooks.db`, `approvals.db`, `ob1_brain.db`, `viking_memory/memories.db`) — active SQLite stores with live WAL files, all in current use by running services.
- **`carrier/silent_running_state.json`, `hardening_loop_state.json`, `local_llm_safety_state.json`, `human_input_queue.json`, `buzz_tg_seen.json`, `buzz_wa_seen.json`, `HOST_ROLE.json`** — actively updated state files (silent_running_state.json was updated within the last minute of this survey running).
- **`carrier/logs/*.log`** (`gateway_watchdog.log` 420 lines/41KB, `fleet_hardening_loop.log` 59 lines, `ob1_brain.log` 348 lines) — all show entries from today (2026-08-26), well under the 7-day staleness threshold; none are empty.
- **`cron/output/*`** — per-run cron output, all from today, actively consulted by monitor-diff logic (`monitor_state.last_output_hash`) in `jobs.json`. Not stale.
- **No `DISPATCH_LOCK` or `SILENT_RUNNING_BRAKE` files exist** in `carrier/` at all — nothing to flag; the governor cron is presently cycling normally (phase=running, cycle 6, last checkpoint seconds before this survey).
- **`docs/` files surveyed** (37 files) — all reference active/recent subsystems (parallel-mate, OB1, Shipwright, billing hard-deny, silent-running doctrine, DISCORD/GITHUB fleet access). No draft/stub markers or clearly-abandoned dates found in the sampled set; a deeper per-file git-log-date pass was out of scope for this pass given the size of the repo (37 docs × individual git log lookups) — recommend as a follow-up survey if LT wants doc-staleness specifically audited.

---

## Notes / Scope Limitations

- This environment's terminal/shell tool was not available to this worker; the survey used read-only file/search tools only (file listing, grep, targeted reads of git ref/packed-refs metadata). Git branch merge-status was inferred from `refs/heads`, `refs/remotes/origin`, and `packed-refs` contents rather than `git branch --merged`, so merge-status conclusions in §2a should be treated as high-confidence but not database-verified.
- Directory disk-size totals (KB/MB estimates) could not be computed precisely without a `du`-equivalent; sizes are qualitative estimates based on file counts and known content type (e.g., `.venv` = large, `.pyc`/`.bak` config files = small).
